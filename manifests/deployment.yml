apiVersion : apps/v1
kind: Deployment
metadata:
  name: "k8cluster-3f5f"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "k8cluster-3f5f"
  template:
    metadata:
      labels:
        app: "k8cluster-3f5f"
    spec:
      containers:
        - name: "k8cluster-3f5f"
          image: "acrdemoregistry123.azurecr.io/k8cluster"
          ports:
          - containerPort: 80